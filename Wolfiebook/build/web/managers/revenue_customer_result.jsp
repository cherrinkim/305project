<%-- 
    Document   : item_suggestions_result
    Created on : Nov 20, 2016, 9:13:32 AM
    Author     : bryan
--%>

<%@page contentType="text/html" pageEncoding="UTF-8"%>
<%@ page import ="java.sql.*" %>
<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
        <link href="../resources/css/manager.css" rel="stylesheet" type="text/css">
        <title>View All Revenue Generated by Customer</title>
    </head>
    <body>
        <% if (session.getAttribute("ismanager") != null) { %>
        <% if ((Boolean) session.getAttribute("ismanager")) { %>
            <jsp:include page="header.jsp"/>
            <h2>View All Revenue Generated by Customer</h2>
            <%
            String id = request.getParameter("id");    
            Class.forName("com.mysql.jdbc.Driver");
            Connection con = DriverManager.getConnection("jdbc:mysql://localhost:3306/Wolfiebook",
            "root", "");
            Statement st = con.createStatement();
            ResultSet rs;
            rs = st.executeQuery("SELECT S.transaction_id, S.buyer_id, S.card_number, S.date_sold, "
                    + "S.advertisement_id, S.number_of_units, S.overseer_id, S.charge_amount "
                    + "FROM Sales S "
                    + "WHERE S.buyer_id = '" + id + "';");
            if (rs.next()) {
                session.setAttribute("queriedid", id);
                rs = st.executeQuery("SELECT first_name, last_name "
                    + "FROM Users "
                    + "WHERE user_id = '" + id + "';");
                String final_name = "dummy";
                if (rs.next()) {
                    final_name = rs.getString(1) + " " + rs.getString(2);
                }
                session.setAttribute("queriedname", final_name);%>
                </br><p>Revenue generated by user #<%=session.getAttribute("queriedid")%> (<%=session.getAttribute("queriedname")%>):</p></br>
                
                <%  
                    rs = st.executeQuery("SELECT S.transaction_id, S.buyer_id, S.card_number, S.date_sold, "
                    + "S.advertisement_id, S.number_of_units, S.overseer_id, S.charge_amount "
                    + "FROM Sales S "
                    + "WHERE S.buyer_id = '" + id + "';");
                    %>
                <table>
                    <tr>
                    <th>Transaction ID</th>
                    <th>Buyer ID</th>
                    <th>Card Number</th>
                    <th>Date Sold</th>
                    <th>Ad ID</th>
                    <th>Units</th>
                    <th>Overseer ID</th>
                    <th>Revenue</th>
                    </tr>
                    <% while (rs.next()) { %>
                    <tr>
                    <td><%= rs.getString(1) %></td>
                    <td><%= rs.getString(2) %></td>
                    <td><%= rs.getString(3) %></td>
                    <td><%= rs.getString(4) %></td>
                    <td><%= rs.getString(5) %></td>
                    <td><%= rs.getString(6) %></td>
                    <td><%= rs.getString(7) %></td>
                    <td>$<%= rs.getString(8) %></td>
                    </tr>
                    
                <% } %>
                </table>
                
            <% } else {
                %> <p> No transactions by that user found.</p> <%
            } %>

            
            <br />
            <br />
            <a href="revenue_customer.jsp">Start a new query</a>
        <% } else { %>
            <div id="error">You do not have permission to access this page.</div>
        <% }} else { %>
            <div id="error">Please <a href="representative_login.jsp">login</a> to access this page.</div>
        <% }; %>
    </body>
</html>
